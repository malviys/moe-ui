---
title: Accordion
description: A vertically stacked set of interactive headings that each reveal a section of content.
---

import { Tabs, TabItem as Tab } from "@astrojs/starlight/components";
import { Steps } from "@astrojs/starlight/components";
import ComponentPreview from "@/components/component-preview.astro";

<Tabs >
  <Tab label="Preview">
    <ComponentPreview name="accordion" />
  </Tab>
  <Tab label="Code">
    ```json doc-gen:file
    {
      "type": "code",
      "file": "./src/registry/preview/accordion/index.tsx",
      "codeblock": {
        "lang": "tsx"
      }
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs>
  <Tab label="CLI">```bash npx @moe/cli add accordion ```</Tab>
  <Tab label="Manual">
<Steps>
<ol>
<li>
  #### Install the following dependency:
  ```bash
  npx expo install @rn-primitives/accordion
  ```
</li>
<li>
  #### Copy/paste the following code to `@/components/ui/accordion.tsx`
  ```json doc-gen:file
  {
    "type": "code",
    "file": "../../packages/registry/src/components/ui/accordion.tsx",
    "codeblock": {
      "lang": "tsx"
    }
  }
  ```
</li>
<li>
  #### Update `tailwind.config.js`
  Add the following animations:
  ```js
  /** @type {import('tailwindcss').Config} */
  module.exports = {
    theme: {
      extend: {
        keyframes: {
          "accordion-down": {
            from: { height: "0" },
            to: { height: "var(--radix-accordion-content-height)" },
          },
          "accordion-up": {
            from: { height: "var(--radix-accordion-content-height)" },
            to: { height: "0" },
          },
        },
        animation: {
          "accordion-down": "accordion-down 0.2s ease-out",
          "accordion-up": "accordion-up 0.2s ease-out",
        },
      },
    },
  };
  ```
</li>
</ol>
</Steps>
  </Tab>
</Tabs>

## Usage

  ```json doc-gen:file
  {
    "type": "code",
    "file": "../../packages/registry/src/components/ui/accordion.tsx",
    "codeblock": {
      "lang": "tsx"
    }
  }
  ```

## Types

### Single

<Tabs>
  <Tab label="Preview">
    <ComponentPreview name="accordion/single" />
  </Tab>
  <Tab label="Code">
    ```json doc-gen:file
    {
      "type": "code",
      "file": "./src/registry/preview/accordion/single.tsx",
      "codeblock": {
        "lang": "tsx"
      }
    }
    ```
  </Tab>
</Tabs>

### Multiple

<Tabs>
  <Tab label="Preview">
    <ComponentPreview name="accordion/multiple" />
  </Tab>
  <Tab label="Code">
    ```json doc-gen:file
    {
      "type": "code",
      "file": "./src/registry/preview/accordion/multiple.tsx",
      "codeblock": {
        "lang": "tsx"
      }
    }
    ```
  </Tab>
</Tabs>

## Props

| Prop            | Type                                  | Default    | Description                                                                          |
| --------------- | ------------------------------------- | ---------- | ------------------------------------------------------------------------------------ |
| `type`          | `'single' \| 'multiple'`              | `'single'` | Whether a single or multiple items can be opened at the same time                    |
| `collapsible`   | `boolean`                             | `false`    | When type is "single", allows closing content when clicking trigger for an open item |
| `value`         | `string \| string[]`                  | -          | The controlled value of the item to expand                                           |
| `onValueChange` | `(value: string \| string[]) => void` | -          | Event handler called when the expanded state changes                                 |
