---
title: Toggle Group
description: A set of two-state buttons that can be toggled on or off.
---

import { Tabs, TabItem as Tab } from "@astrojs/starlight/components";
import ComponentPreview from "@/components/component-preview.astro";

<Tabs>
  <Tab label="Preview">
    <ComponentPreview name="toggle-group"/>
  </Tab>
  <Tab label="Code">
    ```json doc-gen:file
    {
      "type": "code",
      "file": "./src/registry/preview/toggle-group/index.tsx",
      "codeblock": {
        "lang": "tsx"
      }
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs>
  <Tab label="CLI">```bash npx @moe/cli add toggle-group ```</Tab>
  <Tab label="Manual">

### Install the following dependency:

```bash
npx expo install @rn-primitives/toggle-group
```

### Copy/paste the following code to `@/components/ui/toggle-group.tsx`

  ```json doc-gen:file
  {
    "type": "code",
    "file": "../../packages/registry/src/components/ui/toggle-group.tsx",
    "codeblock": {
      "lang": "tsx"
    }
  }
  ```

  </Tab>
</Tabs>

## Usage

  ```json doc-gen:file
  {
    "type": "code",
    "file": "../../packages/registry/src/components/ui/toggle-group.tsx",
    "codeblock": {
      "lang": "tsx"
    }
  }
  ```

## Props

| Prop            | Type                                  | Default    | Description                                       |
| --------------- | ------------------------------------- | ---------- | ------------------------------------------------- |
| `type`          | `'single' \| 'multiple'`              | `'single'` | Whether a single or multiple items can be pressed |
| `value`         | `string \| string[]`                  | -          | The controlled value                              |
| `onValueChange` | `(value: string \| string[]) => void` | -          | Event handler called when value changes           |
