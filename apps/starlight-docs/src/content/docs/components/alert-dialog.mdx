---
title: Alert Dialog
description: A modal dialog that interrupts the user with important content and expects a response.
---

import { Tabs, TabItem as Tab } from "@astrojs/starlight/components";
import ComponentPreview from "@/components/component-preview.astro";

<Tabs>
  <Tab label="Preview">
    <ComponentPreview name="alert-dialog"/>
  </Tab>
  <Tab label="Code">
    ```json doc-gen:file
    {
      "type": "code",
      "file": "./src/registry/preview/alert-dialog/index.tsx",
      "codeblock": {
        "lang": "tsx"
      }
    }
    ```
  </Tab>
</Tabs>

## Installation

<Tabs>
  <Tab label="CLI">```bash npx @moe/cli add alert-dialog ```</Tab>
  <Tab label="Manual">

### Install the following dependency:

```bash
npx expo install @rn-primitives/alert-dialog
```

### Copy/paste the following code to `@/components/ui/alert-dialog.tsx`

  ```json doc-gen:file
  {
    "type": "code",
    "file": "../../packages/registry/src/components/ui/alert-dialog.tsx",
    "codeblock": {
      "lang": "tsx"
    }
  }
  ```

  </Tab>
</Tabs>

## Usage

  ```json doc-gen:file
  {
    "type": "code",
    "file": "../../packages/registry/src/components/ui/alert-dialog.tsx",
    "codeblock": {
      "lang": "tsx"
    }
  }
  ```

## Props

| Prop           | Type                      | Default | Description                                      |
| -------------- | ------------------------- | ------- | ------------------------------------------------ |
| `open`         | `boolean`                 | -       | The controlled open state of the dialog          |
| `onOpenChange` | `(open: boolean) => void` | -       | Event handler called when the open state changes |
