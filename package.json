{
  "name": "moe-ui",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "turbo format",
    "types:check": "turbo types:check",
    "dev:docs": "turbo dev --filter=docs",
    "build:docs": "turbo build --filter=docs",
    "preview:docs": "turbo preview --filter=docs",
    "deploy:docs": "turbo deploy --filter=docs"
  },
  "peerDependencies": {},
  "dependencies": {},
  "devDependencies": {
    "turbo": "^2.8.9"
  }
}